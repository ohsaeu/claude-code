## ❯ pnpm workspace를 사용한 모노레포 구조를 생성해줘.
packages:  
- web (Next.js frontend)
- api (NestJS backend)
- shared (공통 타입 정의)
- ui (공통 ui 컴포넌트)

## ❯ 다음 설정 파일들을 생성해줘
1. TypeScript 설정 (strict mode)
2. ESLint + Prettier 설정
3. Git hooks (Husky + lint-staged)
4. VS Code 프로젝트 설정
